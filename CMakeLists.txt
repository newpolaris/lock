CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(BENCHMARK_ENABLE_TESTING OFF CACHE BOOL "TURN OFF EXAMPLE" FORCE)
ADD_SUBDIRECTORY(benchmark)
ADD_SUBDIRECTORY(test)

PROJECT(spinlock)
ADD_EXECUTABLE(spinlock main.cpp)
TARGET_LINK_LIBRARIES(spinlock benchmark::benchmark)
